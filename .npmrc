# Tauri ships its own toolchain; keep pnpm from trying to be clever.
auto-install-peers=true
strict-peer-dependencies=false
# Electron/Tauri style installs are much faster when the store is hard-linked.
prefer-frozen-lockfile=true
